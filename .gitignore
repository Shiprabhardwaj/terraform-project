# Local .terraform directory (contains provider plugins and modules)
.terraform/

# State files (contain sensitive plain-text data like passwords)
*.tfstate
*.tfstate.*

# Crash logs
crash.log

# Variables files (often contain secrets)
*.tfvars
*.tfvars.json

# Override files
override.tf
override.tf.json


# CLI configuration
.terraformrc
terraform.rc